import type { backendInterface } from "../backend";
import {
  Genre,
  Language,
  OrderStatus,
  SortField,
  SortOrder,
  UserRole,
  Variant_bengali_english,
  Variant_helpfulness_recency_rating,
} from "../backend";

const sampleProduct = {
  id: BigInt(1),
  coverImageUrl: "https://placehold.co/200x280/001a3e/ff8c00?text=Book",
  info: {
    titleEn: "Pather Panchali",
    titleBn: "পথের পাঁচালী",
    authorEn: "Bibhutibhushan Bandyopadhyay",
    authorBn: "বিভূতিভূষণ বন্দ্যোপাধ্যায়",
    descriptionEn: "A timeless Bengali classic about the adventures of Apu.",
    descriptionBn: "অপুর অ্যাডভেঞ্চার নিয়ে এক চিরন্তন বাংলা ক্লাসিক।",
  },
  isbn: "978-81-7066-000-1",
  publisher: "Signet Press",
  createdAt: BigInt(1700000000000),
  priceInPaisa: BigInt(35000),
  language: Language.bengali,
  averageRating: 4.7,
  stockCount: BigInt(15),
  genre: Genre.bengaliClassics,
  publicationDate: BigInt(1945000000000),
  reviewCount: BigInt(120),
};

const sampleProduct2 = {
  id: BigInt(2),
  coverImageUrl: "https://placehold.co/200x280/001a3e/ff8c00?text=Novel",
  info: {
    titleEn: "Feluda: The Complete Adventures",
    titleBn: "ফেলুদা: সম্পূর্ণ অ্যাডভেঞ্চার",
    authorEn: "Satyajit Ray",
    authorBn: "সত্যজিৎ রায়",
    descriptionEn: "Complete detective adventures of Feluda.",
    descriptionBn: "ফেলুদার সম্পূর্ণ গোয়েন্দা অ্যাডভেঞ্চার।",
  },
  isbn: "978-81-7066-111-2",
  publisher: "Ananda Publishers",
  createdAt: BigInt(1700000000000),
  priceInPaisa: BigInt(59900),
  language: Language.bengali,
  averageRating: 4.9,
  stockCount: BigInt(8),
  genre: Genre.fiction,
  publicationDate: BigInt(1965000000000),
  reviewCount: BigInt(340),
};

const sampleProduct3 = {
  id: BigInt(3),
  coverImageUrl: "https://placehold.co/200x280/001a3e/ff8c00?text=Science",
  info: {
    titleEn: "A Brief History of Time",
    titleBn: "সময়ের সংক্ষিপ্ত ইতিহাস",
    authorEn: "Stephen Hawking",
    authorBn: "স্টিফেন হকিং",
    descriptionEn: "A landmark volume in science writing.",
    descriptionBn: "বিজ্ঞান লেখার একটি মাইলফলক।",
  },
  isbn: "978-0-553-38016-3",
  publisher: "Bantam Books",
  createdAt: BigInt(1700000000000),
  priceInPaisa: BigInt(42000),
  language: Language.english,
  averageRating: 4.6,
  stockCount: BigInt(25),
  genre: Genre.science,
  publicationDate: BigInt(570326400000),
  reviewCount: BigInt(89),
};

const sampleFlashSale = {
  id: BigInt(1),
  startTime: BigInt(Date.now() - 3600000),
  endTime: BigInt(Date.now() + 7200000),
  createdAt: BigInt(Date.now() - 7200000),
  isCurrentlyActive: true,
  items: [
    {
      discountedPriceInPaisa: BigInt(24500),
      discountPercent: BigInt(30),
      productId: BigInt(1),
      quantityLimit: BigInt(5),
      originalPriceInPaisa: BigInt(35000),
      soldCount: BigInt(3),
    },
  ],
  titleBn: "ফ্ল্যাশ সেল - বিশেষ ছাড়",
  titleEn: "Flash Sale - Special Discount",
};

export const mockBackend: backendInterface = {
  _initializeAccessControl: async () => undefined,
  addAddress: async () => ({ __kind__: "ok" as const, ok: BigInt(1) }),
  addToCart: async () => ({ __kind__: "ok" as const, ok: null }),
  addToWishlist: async () => ({ __kind__: "ok" as const, ok: null }),
  askQuestion: async () => ({ __kind__: "ok" as const, ok: BigInt(1) }),
  assignCallerUserRole: async () => undefined,
  clearCart: async () => undefined,
  createCheckoutSession: async () => "mock_session_id",
  createFlashSale: async () => BigInt(1),
  createOrder: async () => ({ __kind__: "ok" as const, ok: BigInt(1) }),
  createProduct: async () => ({ __kind__: "ok" as const, ok: BigInt(4) }),
  adminApproveReview: async () => ({ __kind__: "ok" as const, ok: true }),
  adminDeleteReview: async () => ({ __kind__: "ok" as const, ok: true }),
  createPromoCode: async () => ({ __kind__: "ok" as const, ok: BigInt(1) }),
  createReview: async () => ({ __kind__: "ok" as const, ok: BigInt(1) }),
  deactivateFlashSale: async () => true,
  deactivatePromoCode: async () => true,
  deleteAddress: async () => true,
  deleteProduct: async () => true,
  downloadInvoice: async () => "data:application/pdf;base64,mock_invoice_data",
  getAdminAnalytics: async () => ({
    totalProducts: BigInt(42),
    totalOrders: BigInt(218),
    recentRevenueInPaisa: BigInt(1250000),
    recentOrderCount: BigInt(18),
    bestsellers: [[BigInt(2), BigInt(45)], [BigInt(1), BigInt(38)]],
    totalUsers: BigInt(95),
    totalRevenueInPaisa: BigInt(8750000),
  }),
  getCallerUserProfile: async () => ({
    preferredLanguage: Variant_bengali_english.english,
    name: "Rahul Das",
    createdAt: BigInt(1700000000000),
    email: "rahul@example.com",
    phone: "9876543210",
  }),
  getCallerUserRole: async () => UserRole.user,
  getCart: async () => [
    {
      productId: BigInt(1),
      priceSnapshotInPaisa: BigInt(35000),
      addedAt: BigInt(Date.now()),
      quantity: BigInt(1),
    },
  ],
  getFlashSale: async () => sampleFlashSale,
  getOrder: async () => ({
    id: BigInt(1),
    totalInPaisa: BigInt(35000),
    status: OrderStatus.shipped,
    userId: { toText: () => "mock-user" } as any,
    discountInPaisa: BigInt(0),
    promoCodeApplied: undefined,
    idempotencyKey: "mock-user:pi_mock_123",
    createdAt: BigInt(Date.now() - 4 * 86400000),
    estimatedDeliveryDate: BigInt(Date.now() + 3 * 86400000),
    courierNote: "Shipped via DTDC. Tracking ID: DTDC9834567. Expected delivery in 3 days.",
    statusHistory: [
      { status: OrderStatus.processing, note: "Order placed and payment confirmed", updatedAt: BigInt(Date.now() - 4 * 86400000) },
      { status: OrderStatus.shipped, note: "Packed and handed over to DTDC courier", updatedAt: BigInt(Date.now() - 2 * 86400000) },
    ],
    updatedAt: BigInt(Date.now() - 2 * 86400000),
    shippingAddress: {
      id: BigInt(1),
      city: "Purba Bardhaman",
      userId: { toText: () => "mock-user" } as any,
      createdAt: BigInt(Date.now()),
      fullName: "Anirban Ray",
      line1: "Balgona, GT Road",
      line2: "",
      district: "Purba Bardhaman",
      state: "West Bengal",
      isDefault: true,
      phone: "9475727810",
      pincode: "713125",
    },
    stripePaymentIntentId: "pi_mock_123",
    items: [{ priceInPaisa: BigInt(35000), productId: BigInt(1), quantity: BigInt(1), titleEn: "Pather Panchali" }],
  }),
  getOrderedQuantityReport: async () => [
    { productId: BigInt(1), productTitle: "Pather Panchali", totalOrdered: BigInt(12), totalRevenue: BigInt(300000) },
    { productId: BigInt(2), productTitle: "Feluda Vol 1", totalOrdered: BigInt(8), totalRevenue: BigInt(360000) },
  ],
  getProduct: async () => sampleProduct,
  getRecentlyViewed: async () => [sampleProduct2, sampleProduct3],
  getReview: async () => ({
    id: BigInt(1),
    userId: { toText: () => "mock-user" } as any,
    createdAt: BigInt(Date.now()),
    bodyEn: "A fantastic read! The writing is beautiful and the story is moving.",
    productId: BigInt(1),
    isVerifiedPurchase: true,
    rating: BigInt(5),
    helpfulVotes: BigInt(12),
    titleEn: "Classic masterpiece",
  }),
  getStripeSessionStatus: async () => ({
    __kind__: "completed",
    completed: { userPrincipal: "mock-user", response: "{}" },
  }),
  getUserProfile: async () => null,
  getWishlist: async () => [BigInt(2), BigInt(3)],
  isCallerAdmin: async () => false,
  isStripeConfigured: async () => false,
  listAddresses: async () => [
    {
      id: BigInt(1),
      city: "Bardhaman",
      userId: { toText: () => "mock-user" } as any,
      createdAt: BigInt(Date.now()),
      fullName: "Rahul Das",
      line1: "12 Tagore Road",
      line2: "Near Curzon Gate",
      district: "Purba Bardhaman",
      state: "West Bengal",
      isDefault: true,
      phone: "9876543210",
      pincode: "713101",
    },
  ],
  listAllOrders: async () => [{
    id: BigInt(1),
    totalInPaisa: BigInt(35000),
    status: OrderStatus.shipped,
    userId: { toText: () => "mock-user" } as any,
    discountInPaisa: BigInt(0),
    promoCodeApplied: undefined,
    idempotencyKey: "mock-user:pi_mock_123",
    createdAt: BigInt(Date.now() - 4 * 86400000),
    estimatedDeliveryDate: BigInt(Date.now() + 3 * 86400000),
    courierNote: "Shipped via DTDC. Tracking ID: DTDC9834567.",
    statusHistory: [
      { status: OrderStatus.processing, note: "Order placed", updatedAt: BigInt(Date.now() - 4 * 86400000) },
      { status: OrderStatus.shipped, note: "Shipped via DTDC", updatedAt: BigInt(Date.now() - 2 * 86400000) },
    ],
    updatedAt: BigInt(Date.now() - 2 * 86400000),
    shippingAddress: {
      id: BigInt(1),
      city: "Purba Bardhaman",
      userId: { toText: () => "mock-user" } as any,
      createdAt: BigInt(Date.now()),
      fullName: "Anirban Ray",
      line1: "Balgona, GT Road",
      line2: "",
      district: "Purba Bardhaman",
      state: "West Bengal",
      isDefault: true,
      phone: "9475727810",
      pincode: "713125",
    },
    stripePaymentIntentId: "pi_mock_123",
    items: [{ priceInPaisa: BigInt(35000), productId: BigInt(1), quantity: BigInt(1), titleEn: "Pather Panchali" }],
  }],
  listAllEnquiries: async () => [{
    id: "enq-1",
    status: "new" as any,
    name: "Ramesh Mondal",
    submittedAt: BigInt(Date.now() - 86400000),
    email: "ramesh@example.com",
    message: "Do you have the latest edition of Feluda Samagra? Also interested in Byomkesh collection.",
    phone: "9876543210",
    aiReply: "",
    enquiryType: "general",
  }],
  submitChatEnquiry: async () => ({ __kind__: "ok" as const, ok: { aiReply: "Thank you for your enquiry! We have noted your question and will get back to you shortly.", enquiryId: "mock-chat-enq-1" } }),
  submitEnquiry: async () => ({ __kind__: "ok" as const, ok: "mock-enquiry-id" }),
  updateEnquiryStatus: async () => true,
  deleteEnquiry: async () => true,
  getEnquiryCount: async () => BigInt(1),
  listAllEnquiriesPaged: async () => ({ hasMore: false, totalCount: BigInt(1), items: [] }),
  listAllReturns: async () => [],
  listMyReviews: async () => [],
  deleteMyReview: async () => ({ __kind__: "ok" as const, ok: true }),
  getAnalyticsEvents: async () => [
    { userId: "mock-user", eventType: "purchase", timestamp: BigInt(Date.now() - 3600000), productId: BigInt(1), orderId: BigInt(1), amount: BigInt(35000) },
    { userId: "mock-user-2", eventType: "review", timestamp: BigInt(Date.now() - 7200000), productId: BigInt(2), orderId: undefined, amount: undefined },
  ],
  listAllReviews: async () => [
    {
      id: BigInt(1),
      userId: { toText: () => "mock-user" } as any,
      createdAt: BigInt(Date.now() - 5 * 86400000),
      bodyEn: "A fantastic read! The writing is beautiful.",
      productId: BigInt(1),
      isVerifiedPurchase: true,
      isApproved: true,
      rating: BigInt(5),
      helpfulVotes: BigInt(12),
      titleEn: "Classic masterpiece",
    },
  ],
  listAllReviewsPaged: async () => ({ hasMore: false, totalCount: BigInt(1), items: [] }),
  listAnswers: async () => [
    {
      id: BigInt(1),
      createdAt: BigInt(Date.now()),
      questionId: BigInt(1),
      helpfulVotes: BigInt(5),
      answerText: "Yes, it is available in Bengali.",
      answeredBy: { toText: () => "mock-user" } as any,
    },
  ],
  listFlashSales: async () => [sampleFlashSale],
  listMyOrders: async () => [{
    id: BigInt(1),
    totalInPaisa: BigInt(35000),
    status: OrderStatus.shipped,
    userId: { toText: () => "mock-user" } as any,
    discountInPaisa: BigInt(0),
    promoCodeApplied: undefined,
    idempotencyKey: "mock-user:pi_mock_123",
    createdAt: BigInt(Date.now() - 4 * 86400000),
    estimatedDeliveryDate: BigInt(Date.now() + 3 * 86400000),
    courierNote: "Shipped via DTDC. Tracking ID: DTDC9834567.",
    statusHistory: [
      { status: OrderStatus.processing, note: "Order placed", updatedAt: BigInt(Date.now() - 4 * 86400000) },
      { status: OrderStatus.shipped, note: "Shipped via DTDC", updatedAt: BigInt(Date.now() - 2 * 86400000) },
    ],
    updatedAt: BigInt(Date.now() - 2 * 86400000),
    shippingAddress: {
      id: BigInt(1),
      city: "Purba Bardhaman",
      userId: { toText: () => "mock-user" } as any,
      createdAt: BigInt(Date.now()),
      fullName: "Anirban Ray",
      line1: "Balgona, GT Road",
      line2: "",
      district: "Purba Bardhaman",
      state: "West Bengal",
      isDefault: true,
      phone: "9475727810",
      pincode: "713125",
    },
    stripePaymentIntentId: "pi_mock_123",
    items: [{ priceInPaisa: BigInt(35000), productId: BigInt(1), quantity: BigInt(1), titleEn: "Pather Panchali" }],
  }],
  listProducts: async () => [sampleProduct, sampleProduct2, sampleProduct3],
  listPromoCodes: async () => [
    {
      id: BigInt(1),
      code: "VIDYA10",
      discountPercent: BigInt(10),
      maxUsageCount: BigInt(100),
      usageCount: BigInt(5),
      minSpendInPaisa: BigInt(50000),
      validFrom: BigInt(Date.now() - 30 * 86400000),
      validUntil: BigInt(Date.now() + 60 * 86400000),
      isActive: true,
      createdAt: BigInt(Date.now() - 30 * 86400000),
    },
  ],
  listQuestions: async () => [
    {
      id: BigInt(1),
      createdAt: BigInt(Date.now()),
      productId: BigInt(1),
      questionText: "Is this book available in Bengali?",
      askedBy: { toText: () => "mock-user" } as any,
    },
  ],
  listReviews: async () => [
    {
      id: BigInt(1),
      userId: { toText: () => "mock-user" } as any,
      createdAt: BigInt(Date.now() - 5 * 86400000),
      bodyEn: "A fantastic read! The writing is beautiful and the story is moving. Highly recommended for all lovers of Bengali literature.",
      productId: BigInt(1),
      isVerifiedPurchase: true,
      rating: BigInt(5),
      helpfulVotes: BigInt(12),
      titleEn: "Classic masterpiece — a must read",
    },
    {
      id: BigInt(2),
      userId: { toText: () => "mock-user-2" } as any,
      createdAt: BigInt(Date.now() - 15 * 86400000),
      bodyEn: "Beautiful language, deeply moving story. The portrayal of village Bengal is unmatched.",
      productId: BigInt(1),
      isVerifiedPurchase: false,
      rating: BigInt(4),
      helpfulVotes: BigInt(5),
      titleEn: "Beautiful and evocative",
    },
  ],
  listReviewsSorted: async () => [
    {
      id: BigInt(1),
      userId: { toText: () => "mock-user" } as any,
      createdAt: BigInt(Date.now() - 5 * 86400000),
      bodyEn: "A fantastic read! The writing is beautiful and the story is moving.",
      productId: BigInt(1),
      isVerifiedPurchase: true,
      rating: BigInt(5),
      helpfulVotes: BigInt(12),
      titleEn: "Classic masterpiece",
    },
  ],
  postAnswer: async () => ({ __kind__: "ok" as const, ok: BigInt(1) }),
  recordRecentlyViewed: async () => undefined,
  removeFromCart: async () => undefined,
  removeFromWishlist: async () => undefined,
  requestReturn: async () => ({ __kind__: "ok" as const, ok: BigInt(1) }),
  saveCallerUserProfile: async () => ({ __kind__: "ok" as const, ok: null }),
  searchProducts: async () => [sampleProduct, sampleProduct2],
  setDefaultAddress: async () => true,
  setStripeConfiguration: async () => undefined,
  getMyEnquiries: async () => [],
  getTopProducts: async () => [sampleProduct, sampleProduct2, sampleProduct3],
  transform: async (input) => ({ status: BigInt(200), body: input.response.body, headers: [] }),
  updateAddress: async () => ({ __kind__: "ok" as const, ok: true }),
  updateCartQuantity: async () => ({ __kind__: "ok" as const, ok: null }),
  updateOrderStatus: async () => true,
  updateProduct: async () => ({ __kind__: "ok" as const, ok: true }),
  updatePromoCode: async () => ({ __kind__: "ok" as const, ok: true }),
  updateStock: async () => true,
  validatePromoCode: async () => null,
  voteAnswerHelpful: async () => ({ __kind__: "ok" as const, ok: true }),
  voteReviewHelpful: async () => ({ __kind__: "ok" as const, ok: true }),
  recordDownload: async () => undefined,
  getDownloadStats: async () => [],
};
